
public class SchoolTeamFlag {

	/*
BufferedReader reader = new BufferedReader(new InputStreamReader(System.in))
int t = reader.readLine() as int

t.times{
    String[] line = reader.readLine().split(' ')
    int n = line[0] as int, m = line[1] as int
        
    String answer = 'YES'
    String colourBefore = null
    n.times {
        String lineValues = reader.readLine()
        if('YES'.equals(answer)) {
            Set lineColours = new HashSet() 
            m.times {
                lineColours.add(lineValues.substring(it, it+1))
            }

            String lineColour = lineColours.iterator().next()
            if(lineColours.size() == 1 && (colourBefore == null || !colourBefore.equals(lineColour))) {
                colourBefore = lineColour
            } else {
                answer = 'NO'
            }
        }        
    }
    println answer
}
*/}
